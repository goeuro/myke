---
project: shell
desc: test shell-related functionality
tasks:
  shell:
    cmd: echo
  subshell:
    cmd: echo $(echo subshell works)
  error:
    cmd: foobar
  echo:
    cmd: echo
  interpolate:
    cmd: |-
      foo=value_from_task
      echo $(echo envvar from subshell is $foo)
