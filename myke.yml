---
project: myke
desc: make with yml
discover:
  - examples
tasks:
  test:
    desc: run as myke test (or) myke myke/test
    cmd: go test -v ./...
  package:
    cmd: cross-compile.sh
    before: myke test
